name: pytest
on: workflow_dispatch

jobs:
  
  pytest:
    runs-on: ubuntu-latest
    steps:
      - name: clone repository
        run: |
          git clone https://github.com/justing888/Testaction.git
          cd Testaction
      - name: install pytest
        run: pip install pytest
      - name: run pytest
        run: pytest
